version: '3.8'

services:
  gateway:
    image: ghcr.io/wiktorczetyrbok/microservices-monoliths-comp-images/js-mono:latest
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - "5000:8080"